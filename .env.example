# Google Maps browser key used by the class location picker.
# Enable "Maps JavaScript API" and "Geocoding API" on the key, and restrict it
# to your origins (http://localhost:5173/*, http://localhost:5174/*).
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-browser-key
